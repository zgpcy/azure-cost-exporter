# Azure Credentials Secret Template
#
# IMPORTANT: This file contains sensitive credentials!
# 1. Replace the base64-encoded placeholder values with your actual credentials
# 2. Do NOT commit this file to version control after adding real credentials
# 3. Consider using sealed-secrets, external-secrets, or Vault for production
#
# To encode credentials:
#   echo -n "your-client-id" | base64
#   echo -n "your-client-secret" | base64
#   echo -n "your-tenant-id" | base64

apiVersion: v1
kind: Secret
metadata:
  name: azure-credentials
  namespace: monitoring
type: Opaque
data:
  # Azure Service Principal Client ID
  # Replace with: echo -n "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" | base64
  client-id: "REPLACE_ME"

  # Azure Service Principal Client Secret
  # Replace with: echo -n "your-secret-value" | base64
  client-secret: "REPLACE_ME"

  # Azure Tenant ID
  # Replace with: echo -n "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx" | base64
  tenant-id: "REPLACE_ME"

---
# Alternative: Using Azure Workload Identity (recommended for AKS)
# If using Workload Identity, you don't need the client-secret
# Instead, annotate the ServiceAccount:
#
# apiVersion: v1
# kind: ServiceAccount
# metadata:
#   name: azure-cost-exporter
#   namespace: monitoring
#   annotations:
#     azure.workload.identity/client-id: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
#   labels:
#     azure.workload.identity/use: "true"
#
# And add label to the Pod spec:
#   labels:
#     azure.workload.identity/use: "true"
